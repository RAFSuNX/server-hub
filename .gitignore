# Claude guidance (local only)
CLAUDE.md

# Environment secrets
.env

# NixOS secrets (age-encrypted files are safe to commit — do NOT add nixos/secrets/ here)
# nixos/secrets/  <- intentionally not ignored

# Kubernetes secrets
k8s/secrets/
k8s/media/*-secret.yaml
k8s/media/*-secrets.yaml
k8s/tools/*-secret.yaml
k8s/tools/*-secrets.yaml

# Kubeconfig
*.kubeconfig
kubeconfig

# Ansible vault password (never commit)
debian/.vault-pass
.vault-pass

# Server details (contains real IPs and infrastructure info)
data/

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
talos/
