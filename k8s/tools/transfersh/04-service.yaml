---
apiVersion: v1
kind: Service
metadata:
  name: transfersh
  namespace: tools
spec:
  type: ClusterIP
  selector:
    app: transfersh
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
