---
apiVersion: v1
kind: Service
metadata:
  name: firefox
  namespace: tools
spec:
  type: ClusterIP
  selector:
    app: firefox
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: https
