---
apiVersion: v1
kind: Service
metadata:
  name: sigserver
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: sigserver
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
