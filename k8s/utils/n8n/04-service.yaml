---
apiVersion: v1
kind: Service
metadata:
  name: n8n-service
  namespace: utils
spec:
  type: ClusterIP
  clusterIP: 10.43.1.7
  selector:
    app: n8n
  ports:
    - port: 80
      targetPort: 5678
      protocol: TCP
