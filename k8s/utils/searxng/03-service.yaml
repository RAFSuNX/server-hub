---
apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: utils
spec:
  type: ClusterIP
  clusterIP: 10.43.1.8
  selector:
    app: searxng
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
