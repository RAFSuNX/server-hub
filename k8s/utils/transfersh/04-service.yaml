---
apiVersion: v1
kind: Service
metadata:
  name: transfersh
  namespace: utils
spec:
  clusterIP: 10.43.1.6
  type: ClusterIP
  selector:
    app: transfersh
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
