---
apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma-service
  namespace: utils
spec:
  type: ClusterIP
  clusterIP: 10.43.1.9
  selector:
    app: uptime-kuma
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
