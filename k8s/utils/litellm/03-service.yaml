---
apiVersion: v1
kind: Service
metadata:
  name: litellm-service
  namespace: utils
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
