---
apiVersion: v1
kind: Service
metadata:
  name: kite
  namespace: kube-system
spec:
  type: ClusterIP
  selector:
    app: kite
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
