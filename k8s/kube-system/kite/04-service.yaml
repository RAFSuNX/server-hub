---
apiVersion: v1
kind: Service
metadata:
  name: kite
  namespace: kube-system
spec:
  clusterIP: 10.43.1.2
  type: ClusterIP
  selector:
    app: kite
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
