---
apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: media
spec:
  clusterIP: 10.43.1.10
  type: ClusterIP
  selector:
    app: jellyfin
  ports:
    - port: 80
      targetPort: 8096
      protocol: TCP
