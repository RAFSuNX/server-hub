apiVersion: apps/v1
kind: Deployment
metadata:
  name: her-server
  namespace: her
spec:
  replicas: 2
  selector:
    matchLabels:
      app: her-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0  # never drop below current replica count during deploy
      maxSurge: 1        # spin up 1 extra pod, wait for ready, then kill 1 old
  template:
    metadata:
      labels:
        app: her-server
    spec:
      # Must be > uvicorn --timeout-graceful-shutdown (120s) + preStop sleep (10s).
      terminationGracePeriodSeconds: 150
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: her-server
          image: ghcr.io/rafsunx/her-server:sha-2fb3ca3
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: her-server-config
            - secretRef:
                name: her-server-secrets
          env:
            # 4 workers × 2 replicas = 8 workers total.
            # DB connections: budget(40) / workers(4) = 10/worker × 8 = 80 connections ≤ 200.
            - name: WORKERS
              value: "4"
          lifecycle:
            preStop:
              exec:
                # Gives iptables rules time to propagate before SIGTERM fires.
                # When k8s removes a pod from Service endpoints, kube-proxy updates
                # iptables on all nodes — this takes ~2-5s. Without this sleep,
                # cloudflared still routes new requests to the terminating pod
                # during that window and they fail.
                command: ["sleep", "10"]
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "4Gi"
