apiVersion: v1
kind: ServiceAccount
metadata:
  name: her-log-collector
  namespace: her
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: her-log-reader
rules:
  - apiGroups: [""]
    resources: [pods, pods/log]
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: her-log-reader
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: her-log-reader
subjects:
  - kind: ServiceAccount
    name: her-log-collector
    namespace: her
---
# Logs accumulate indefinitely — delete files manually inside the pod when needed.
# reclaimPolicy Retain means the PV survives even if the PVC is deleted.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: her-logs
  namespace: her
  annotations:
    # Remind operators: data is NOT auto-deleted. Manual cleanup only.
    her.app/retention: "manual"
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: her-log-collector
  namespace: her
spec:
  replicas: 1
  selector:
    matchLabels:
      app: her-log-collector
  template:
    metadata:
      labels:
        app: her-log-collector
    spec:
      serviceAccountName: her-log-collector
      # Run as root so we can always write to the Longhorn volume regardless of fsGroup.
      securityContext:
        runAsUser: 0
        runAsGroup: 0
      terminationGracePeriodSeconds: 10
      containers:
        - name: collector
          image: bitnami/kubectl:latest
          command:
            - /bin/sh
            - -c
            - |
              set -e
              mkdir -p /logs

              log() {
                echo "[$(date -u +%FT%TZ)] $*"
              }

              LOG_FILE="/logs/her-$(date +%F).log"
              log "=== Log collector started ===" >> "$LOG_FILE"

              # kubectl logs --follow gets ALL logs since pod start then streams new ones.
              # On restart we get some duplicates (from pod start) but never gaps.
              # Append-only (>>) so old files are never touched or deleted.
              while true; do
                CURRENT_DATE=$(date +%F)
                LOG_FILE="/logs/her-${CURRENT_DATE}.log"

                log "Streaming her-server logs → $LOG_FILE" >> "$LOG_FILE"

                kubectl logs \
                  -n her \
                  -l app=her-server \
                  --all-containers \
                  --prefix \
                  --timestamps \
                  --follow \
                  2>&1 \
                | grep -v '"GET /health HTTP' \
                | tee -a "$LOG_FILE" &
                PID=$!

                # Check every 60s for midnight (date change) — rotate to new file.
                # Killing the kubectl process causes the outer loop to restart it,
                # picking up the new LOG_FILE path.
                while kill -0 $PID 2>/dev/null; do
                  sleep 60
                  NEW_DATE=$(date +%F)
                  if [ "$NEW_DATE" != "$CURRENT_DATE" ]; then
                    log "Date changed to $NEW_DATE — rotating log file" >> "$LOG_FILE"
                    kill $PID 2>/dev/null
                    break
                  fi
                done

                wait $PID 2>/dev/null || true
                sleep 2
              done
          volumeMounts:
            - name: logs
              mountPath: /logs
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: logs
          persistentVolumeClaim:
            claimName: her-logs
