---
# Static fallback used when this host isn't targeted in the current play run
# (so hostvars lookups from other hosts' templates still resolve). The k3s
# role overwrites this with a live-queried fact when systemc is in-play.
tailscale_node_ip: 100.69.65.52
