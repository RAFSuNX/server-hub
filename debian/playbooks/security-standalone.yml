---
# Security baseline for standalone servers (no k3s). SSH stays on port 22.
- name: Security baseline (standalone)
  hosts: standalone
  become: true
  roles:
    - base
    - ssh
    - firewall
    - fail2ban
