apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: storage
  namespace: flux-system
spec:
  interval: 5m
  path: ./k8s/storage
  prune: true
  sourceRef:
    kind: GitRepository
    name: server-hub
  decryption:
    provider: sops
    secretRef:
      name: sops-age
